{% extends 'base.html.twig' %}

{% block title %}Redéfinissez votre mot de passe{% endblock %}

{% block body %}
    <h1>Redéfinissez votre mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Redéfinir le mot de passe</button>
    {{ form_end(resetForm) }}
{% endblock %}
